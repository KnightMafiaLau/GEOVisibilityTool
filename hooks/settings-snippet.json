{
  "_comment": "把以下 hooks 块合并到你的 ~/.claude/settings.json。如果已有 hooks 字段,合并 PreToolUse 数组即可。<REPO> 替换成 GEOVisibilityTool 仓库的绝对路径。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "<REPO>/hooks/block-self-generated-reports.sh"
          }
        ]
      }
    ]
  }
}
